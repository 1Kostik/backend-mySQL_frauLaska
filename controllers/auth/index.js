const { login, userFindById } = require("./login");
const logout = require("./logout");
// const register = require("./register");

module.exports = {
  login,
  logout,
  //  register,
  userFindById,
};
